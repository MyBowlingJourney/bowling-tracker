# My Bowling Journey 🎳

A mobile-first bowling tracker for league bowlers — scores, shot-by-shot
detail, team standings, side pots and season stats. Built for a real
Tuesday night: one phone, one thumb, four teammates to keep score for, and
a wifi signal that comes and goes.

Live at **[mybowlingjourney.com](https://mybowlingjourney.com)**, and as an
Android app.

Published by My Bowling Journey LLC.

---

## What it does

**Log a night however you want to.** Four modes, chosen per session:

| | |
|---|---|
| **A practice session** | Drills and the detail fields |
| **A league night** | Money games and the team roster |
| **A tournament** | Blocks and the cut line |
| **A game or two out** | Scores and standings only |

Track three numbers or every delivery. Score for yourself or for the whole
team from one device. Import a scorecard by photographing the monitor.

**It works at the lanes.** Everything is local-first: scores are written
to the device immediately and synced when there's signal. A failed write
is queued, retried in order, and survives a reload — a night logged in a
basement alley with no bars is not a night lost.

**The stats are the point.** Strike and spare percentages, carry by ball,
frame position, split conversion, ten-pin leaves, running averages,
theoretical scores, and where your game is actually costing you pins.
Cards that need shot-by-shot data hide themselves when it isn't there,
rather than showing a screen of zeroes.

---

## Running it

```bash
npm ci
npm run dev      # local dev server
npm test         # unit tests
npm run build    # production build
```

Local runs need a `.env` at the repo root — in CI these come from
repository secrets, so it is easy to forget they are needed at all:

```
VITE_SUPABASE_URL=...
VITE_SUPABASE_ANON_KEY=...
```

The **anon** key, never `service_role`. The anon key is safe in the
browser; row-level security is what protects the data.

Deployment is automatic: merging to `main` builds and publishes to GitHub
Pages, and Supabase Edge Functions deploy on change.

---

## The Android app

Capacitor wraps the same web build. The web app and the app are one
codebase.

```bash
npm run cap:sync        # builds the web app and copies it in
npx cap open android    # opens Android Studio
```

**`appId` is `com.mybowlingjourney.app` and can never change.** After the
first Play Store upload, a different id is a different app: new listing,
zero installs, no upgrade path for anyone who installed the old one.

**Two things about the native shell that are easy to get wrong:**

The WebView loads from **`https://localhost`**, which is a different
origin from the website. Every Edge Function pins
`Access-Control-Allow-Origin`, so that origin must be in the
`ALLOWED_ORIGINS` secret or the app is rejected while the website works
fine.

**Android 15 enforces edge-to-edge**: the WebView renders behind the
status and navigation bars whether it asks to or not. Anything pinned to
the top of the screen needs `env(safe-area-inset-top)`, and
`viewport-fit=cover` must stay in `index.html` or `env()` resolves to
zero and every safe-area rule silently does nothing. Without it the header
sits *under* the status bar, where the system consumes the taps — visible,
and completely dead to touch.

---

## Signing in

A six-digit code, emailed, typed into the app. No passwords.

The same email also carries a link, which always points at the **website**
— never at the app.

That is deliberate. A link aimed at the native shell has to survive the
email client's embedded browser, that browser choosing to hand a custom
scheme back to Android, an intent filter match, and Capacitor delivering
the event. Any one of those drops it silently, and the bowler sees nothing
happen at all. A code typed into an app that is already open skips the lot.

`OTP_LENGTH` in `src/SignIn.jsx` must match Supabase's **Email OTP
Length** (Authentication → Sign In / Providers → Email). It is one
constant because it is not the client's fact to decide — it shipped as 6
while Supabase was sending 8, and the field would not accept a valid code.

Supabase's built-in email sender is capped at **2 messages per hour** and
is documented as not for production. Custom SMTP is configured through
Resend; without it the app can serve two people an hour, total.

---

## Layout

```
src/
  domain/        Pure logic — scoring, stats, money, sync rules.
                 No React, no browser, fully tested.
  *.jsx          Screens and components.
  syncQueue.js   The offline queue and every cloud write.
  scopedStorage.js  Per-user local storage.
  nativeAuth.js  Deep-link handling for the Android shell.

public/               Copied verbatim into dist/ — privacy policy,
                      terms, account deletion page.
android/              Generated by Capacitor. AndroidManifest.xml is
                      real source and is committed.
supabase/functions/   Edge Functions (scorecard import, analysis,
                      centers, the genie, account deletion)
scripts/              Schema tooling, run by CI
schema-snapshot.csv   The live database's shape, regenerated weekly
```

**`src/domain/` is where logic belongs.** If something can be decided
without a screen, it goes there and gets tested. The components render;
they don't compute.

---

## The database

Supabase Postgres, with row-level security on every table.

**`schema-snapshot.csv` is generated, never edited.** A GitHub Action runs
`scripts/audit_schema.sql` against the live database weekly and opens a
pull request when anything has drifted. `scripts/generate_schema_sql.sql`
emits runnable DDL from the same connection.

This matters more than it sounds. A hand-maintained schema file drifted 21
columns on the `shots` table and caused two separate code reviews to report
security holes that had already been fixed, because they read the file as
though it were the database. **If a generated file is wrong, the database
is wrong, or the query is — don't patch the output.**

Setup: add `SUPABASE_DB_URL` (Project Settings → Database → connection
string, session pooler) as a repository secret.

**When you add a table holding personal data, give it a foreign key to
`auth.users` — directly or via `profiles` — with `ON DELETE CASCADE`.**
Account deletion removes one row and lets the graph do the rest, so a
table without that key silently keeps a bowler's data after they asked for
it gone. See [ACCOUNT-DELETION.md](ACCOUNT-DELETION.md).

---

## Verification

`npm test` runs the unit suite. The full harness is broader and lives
outside the repo — 23 checks covering:

- **Scoring**, verified against an independent implementation of the rules
  of bowling across 3,000 random games
- **Money**, as arithmetic: net equals gross minus cost at every level,
  season totals equal the sum of their nights
- **Damaged records** — every domain function called with the shapes a
  partial sync actually produces
- **Every component**, rendered with missing and null data
- **Real-browser suites** in Chromium with real IndexedDB: cross-account
  isolation, the sync queue, duplicate handling, stale deploys

CI additionally runs a data-flow audit and a live-schema audit, both of
which fail the build rather than printing warnings.

**None of that catches a dead touch target.** The header was unusable on
every Android 15 device while rendering perfectly, passing every test, and
responding correctly to clicks dispatched from remote devtools. Some
things are only found by a finger on a real screen.

---

## Conventions worth knowing

**Line endings are mixed** — roughly 138 files CRLF, 84 LF. Match whatever
the file you are editing already uses. Normalising one turns a two-line
change into a whole-file diff and buries the actual edit.

**Generated files are not edited by hand** — `schema-snapshot.csv`,
`schema.sql`, and the audit baselines.

**Guard for type, not truthiness.** `Array.isArray(x) ? x : []` says the
container is a list and nothing about what's in it; a single null row from
an interrupted write has taken down whole screens. Filter the elements too.

**`color: undefined` in a spread is an override, not an absence.**
`{...S.button, color: cond ? C.accent : undefined}` deletes the inherited
colour and React omits the property, so the browser falls back to
near-black — invisible on every dark theme. Name the fallback explicitly.

**A checker that fails on most of what it tries is usually wrong itself.**
The harness says so out loud when it happens.

---

## Diagnostics

Settings → Diagnostics → **Copy diagnostics** produces a redacted report:
crashes, failed cloud writes with their SQLSTATE, and writes that reported
success while changing nothing. Error text is redacted at record time —
Postgres puts real values in its messages, and those values are other
people's names.

---

## Also in this repo

- [ACCOUNT-DELETION.md](ACCOUNT-DELETION.md) — what to do when someone asks
  for their account to be removed, and how deletion works underneath.
